name: auto generate new pr from latest merge
on:
  pull_request:
    branches: ["main"]
jobs:
  auto-merge:
    runs-on: ubuntu-latest
    steps:
    - name: Merge
      uses: KeisukeYamashita/auto-pull-request-merge@v1
      with:
        token: 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
        intervalSeconds: 10
        timeoutSeconds: 30
